{{- /* Default (no active $OMP_PROFILE) omp config. Deep-merged from the
       single source of truth in .chezmoidata/omp.yml, using the same builder
       as each profile config, so there is no drift.
       .work selects which profile is the machine default (see omp.defaults). */ -}}
{{- $default := index .omp.defaults (ternary "work" "personal" .work) -}}
{{ includeTemplate "omp-config" (dict "profile" $default "omp" .omp) }}
